package clinic.client;

import clinic.core.Pacjent;
import clinic.core.Doktor;

public class ReceptionApp {

    public static void main(String[] args) {

        // utworzenie obiektów
        Pacjent pacjent = new Pacjent("Jan", "12345678901");
        Doktor doktor = new Doktor();

        // wypisanie imienia pacjenta
        System.out.println(pacjent.getName());

        // System.out.println(pacjent.pesel);
        // Błąd kompilatora, ponieważ pole pesel jest private w klasie Pacjent.

        // pacjent.setKodChoroby("A01");
        // Błąd kompilatora, ponieważ metoda setKodChoroby ma dostęp package-private
        // i nie jest dostępna poza pakietem clinic.core.

        // poprawne wywołanie diagnozy przez lekarza
        doktor.diagnoza(pacjent, "A01");
    }
}
